import { Props } from './types';
declare const BlogItem: ({
  id,
  image,
  title,
  subtitle,
  paragraph,
  tags,
  variant,
  isMatch,
  setTag,
}: Props) => JSX.Element;
export default BlogItem;
