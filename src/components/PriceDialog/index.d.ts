import { Props } from './types';
declare const PriceDialog: ({
  title,
  price,
  possibilities,
  duration,
  open,
  handleClose,
  setErrorSubmit,
  setSuccessSubmit,
}: Props) => JSX.Element;
export default PriceDialog;
