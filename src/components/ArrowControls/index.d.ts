import { Props } from './types';
declare const ArrowControls: ({
  props: [handleBackward, handleForward, isActiveBackward, isActiveForward],
  isMatch,
}: Props) => JSX.Element;
export default ArrowControls;
