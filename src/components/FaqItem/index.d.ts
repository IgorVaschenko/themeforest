import React from 'react';

import { Props } from './types';
declare const _default: React.MemoExoticComponent<
  ({ isActive, title, description, handleClick }: Props) => JSX.Element
>;
export default _default;
